#include <stdio.h>
int main() {
    struct foo {
              int bar;
                    struct foo *next;

    };
    typedef struct foo foo;

    foo  var1;
    foo *var2;
    foo  var3[5];
    int x = (var3 + 2)->bar;
    int y = (var3 + 4) -> bar;
}
